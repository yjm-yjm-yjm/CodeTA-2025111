FROM golang:1.26-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/template-admin-web-server ./cmd

FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=build /out/template-admin-web-server /app/template-admin-web-server
EXPOSE 8081
ENTRYPOINT ["/app/template-admin-web-server"]
